# Copy to pipeline/.env and paste your key after the = (no quotes, no spaces).
# This file (.env) is gitignored — it is never committed.
RUNWAY_API_KEY=
# optional, for /watch transcripts of no-caption videos:
GROQ_API_KEY=
# Google AI Studio (Veo) key — get at aistudio.google.com/apikey
VEO_API_KEY=
